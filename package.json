{
  "name": "usertypo",
  "private": true,
  "description": "usertypo_ — typing tests, profiles, friends, leaderboards, and multiplayer races",
  "license": "AGPL-3.0-only",
  "homepage": "https://usertypo.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/usertypo/usertypo.git"
  },
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test \"test/*.test.js\"",
    "build:pages": "node scripts/build-page-fragments.js",
    "build:cloudflare": "node scripts/build-cloudflare.js",
    "serve": "npx --yes serve . --single --listen 3000",
    "build:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/tailwind.css --minify",
    "build": "npm run build:css && npm run build:pages",
    "worker:leaderboards:dev": "npm run dev --prefix workers/leaderboards",
    "worker:leaderboards:deploy:dev": "npm run deploy:dev --prefix workers/leaderboards",
    "worker:leaderboards:deploy:prod": "npm run deploy:prod --prefix workers/leaderboards",
    "worker:site-stats:dev": "npm run dev --prefix workers/site-stats",
    "worker:site-stats:deploy:dev": "npm run deploy:dev --prefix workers/site-stats",
    "worker:site-stats:deploy:prod": "npm run deploy:prod --prefix workers/site-stats",
    "worker:multiplayer:dev": "npm run dev --prefix workers/multiplayer",
    "worker:multiplayer:deploy:dev": "npm run deploy:dev --prefix workers/multiplayer",
    "worker:multiplayer:deploy:prod": "npm run deploy:prod --prefix workers/multiplayer",
    "worker:notifications:dev": "npm run dev --prefix workers/notifications",
    "worker:notifications:deploy:dev": "npm run deploy:dev --prefix workers/notifications",
    "worker:notifications:deploy:prod": "npm run deploy:prod --prefix workers/notifications",
    "worker:theme-assets:dev": "npm run dev --prefix workers/theme-assets",
    "worker:theme-assets:deploy:dev": "npm run deploy:dev --prefix workers/theme-assets",
    "worker:theme-assets:deploy:prod": "npm run deploy:prod --prefix workers/theme-assets",
    "worker:admin:dev": "npm run dev --prefix workers/admin",
    "worker:admin:deploy:dev": "npm run deploy:dev --prefix workers/admin",
    "worker:admin:deploy:prod": "npm run deploy:prod --prefix workers/admin"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.10",
    "tailwindcss": "^3.4.17"
  }
}
